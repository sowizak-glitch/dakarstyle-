const CACHE='tijane-future-v5.1.1-public';const PAYLOAD=[...Array(10)].map((_,i)=>`/tijane/payload/p${String(i).padStart(2,'0')}.txt?v=511`);const CORE=['/tijane/','/tijane/index.html','/tijane/manifest.webmanifest?v=511','/tijane/icon.svg',...PAYLOAD];self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(c=>c.addAll(CORE)).then(()=>self.skipWaiting()));});self.addEventListener('activate',event=>{event.waitUntil((async()=>{const keys=await caches.keys();await Promise.all(keys.filter(k=>k.startsWith('tijane-')&&k!==CACHE).map(k=>caches.delete(k)));await self.clients.claim();})());});self.addEventListener('message',event=>{if(event.data?.type==='SKIP_WAITING')self.skipWaiting();if(event.data?.type==='CLEAR_OLD_CACHES')event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('tijane-')&&k!==CACHE).map(k=>caches.delete(k)))));});self.addEventListener('fetch',event=>{const r=event.request;if(r.method!=='GET')return;const u=new URL(r.url);if(u.origin!==self.location.origin||!u.pathname.startsWith('/tijane/'))return;if(r.mode==='navigate'){event.respondWith(fetch(r,{cache:'no-store'}).then(res=>{if(res.ok)caches.open(CACHE).then(c=>c.put('/tijane/index.html',res.clone()));return res;}).catch(()=>caches.match('/tijane/index.html')));return;}event.respondWith(caches.match(r).then(hit=>hit||fetch(r).then(res=>{if(res.ok)caches.open(CACHE).then(c=>c.put(r,res.clone()));return res;})));});