import { DESIGN_TOKENS, escapeHtml } from './social-intelligence-ui-v4.js';

export const FOUNDER_OS_CLIENT_ROUTE = '/founder-os/client.js';

const CSS = `${DESIGN_TOKENS}
*{box-sizing:border-box}
html{background:var(--bg);color:var(--txt);-webkit-text-size-adjust:100%;scroll-behavior:smooth}
body{margin:0;min-height:100dvh;font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:radial-gradient(circle at 92% -10%,rgba(213,181,106,.18),transparent 30%),radial-gradient(circle at 4% 18%,rgba(75,159,153,.12),transparent 27%),linear-gradient(180deg,#0b0b0e,var(--bg) 55%);overflow-x:hidden}
button,a{font:inherit}button{touch-action:manipulation}:focus-visible{outline:2px solid var(--gold2);outline-offset:3px}
.fos-shell{width:min(1600px,100%);margin:auto;min-height:100dvh;display:grid;grid-template-columns:1fr}
.fos-rail{display:none}
.fos-main{min-width:0;padding:12px 12px calc(92px + env(safe-area-inset-bottom))}
.fos-top{position:sticky;top:0;z-index:40;display:flex;align-items:center;justify-content:space-between;gap:10px;padding:9px 0 12px;background:linear-gradient(180deg,rgba(8,8,10,.98),rgba(8,8,10,.88) 75%,transparent);backdrop-filter:blur(18px)}
.fos-brand{display:flex;align-items:center;gap:10px;min-width:0}.fos-mark{width:42px;height:42px;flex:0 0 42px;border-radius:14px;display:grid;place-items:center;background:linear-gradient(145deg,#e2c77e,#b8903f);color:#17130b;font-weight:950;font-size:12px;box-shadow:0 15px 40px rgba(213,181,106,.16)}
.fos-brand strong{display:block;font-size:13px}.fos-brand small{display:block;color:var(--muted);font-size:10px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;max-width:220px}
.fos-top-actions{display:flex;gap:7px}.fos-btn{min-height:44px;border-radius:13px;border:1px solid var(--line);background:var(--panel);color:var(--soft);padding:0 13px;font-size:11px;font-weight:850;cursor:pointer}.fos-btn.gold{border:0;background:linear-gradient(135deg,#e3ca84,#bd9341);color:#181208}.fos-btn.danger{border-color:rgba(255,155,155,.32);color:#ffc1c1;background:rgba(255,155,155,.06)}.fos-btn:disabled{opacity:.5;cursor:not-allowed}
.fos-status-strip{display:flex;gap:7px;overflow-x:auto;scrollbar-width:none;padding:1px 0 12px}.fos-status-strip::-webkit-scrollbar{display:none}.fos-chip{flex:0 0 auto;display:inline-flex;align-items:center;gap:7px;min-height:34px;padding:0 11px;border:1px solid var(--line);border-radius:999px;background:rgba(18,18,23,.88);color:var(--soft);font-size:10px;font-weight:800}.fos-dot{width:7px;height:7px;border-radius:50%;background:var(--good)}.fos-dot.warn{background:var(--warn)}.fos-dot.danger{background:var(--danger)}.fos-dot.muted{background:var(--muted)}
.fos-view{display:none;min-width:0}.fos-view.active{display:block}.fos-hero{position:relative;overflow:hidden;padding:24px 18px;border:1px solid var(--line);border-radius:25px;background:linear-gradient(155deg,#19191f,#0f0f14 72%);box-shadow:var(--shadow)}.fos-hero:after{content:"";position:absolute;width:270px;height:270px;right:-100px;top:-140px;border-radius:50%;background:radial-gradient(circle,rgba(213,181,106,.2),transparent 69%)}
.fos-eyebrow{color:var(--gold);font-size:10px;font-weight:950;letter-spacing:.16em;text-transform:uppercase}.fos-hero h1{font-size:clamp(38px,11vw,66px);line-height:.94;letter-spacing:-.06em;margin:12px 0 14px;max-width:900px}.fos-hero p{color:var(--muted);font-size:14px;line-height:1.6;margin:0;max-width:760px}.fos-hero-actions{display:flex;flex-wrap:wrap;gap:8px;margin-top:18px;position:relative;z-index:2}
.fos-metrics{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:9px}.fos-metric,.fos-panel,.fos-card,.fos-decision,.fos-alert,.fos-agent,.fos-connector{border:1px solid var(--line);background:linear-gradient(180deg,#141419,#0f0f14);border-radius:19px}.fos-metric{padding:13px}.fos-metric small{display:block;color:var(--muted);font-size:9px;text-transform:uppercase;letter-spacing:.07em}.fos-metric b{display:block;font-size:23px;margin-top:7px;letter-spacing:-.04em}.fos-metric b.good{color:var(--good)}.fos-metric b.warn{color:var(--warn)}.fos-metric b.danger{color:var(--danger)}
.fos-grid{display:grid;grid-template-columns:1fr;gap:9px;margin-top:9px}.fos-panel{padding:17px;min-width:0}.fos-panel h2,.fos-card h3{margin:0}.fos-panel h2{font-size:17px}.fos-sub{color:var(--muted);font-size:11px;line-height:1.5;margin-top:4px}.fos-section-head{display:flex;align-items:flex-end;justify-content:space-between;gap:10px;margin:7px 0 14px}.fos-section-head h1{margin:0;font-size:31px;letter-spacing:-.045em}.fos-section-head p{margin:4px 0 0;color:var(--muted);font-size:11px}
.fos-brief{border-color:rgba(213,181,106,.27);background:linear-gradient(145deg,rgba(213,181,106,.07),#101014 60%)}.fos-brief-headline{font-size:clamp(23px,6vw,34px);letter-spacing:-.045em;line-height:1.05;margin:13px 0 8px}.fos-focus{margin-top:14px;padding:14px;border-radius:15px;border:1px solid rgba(75,159,153,.22);background:rgba(75,159,153,.065)}.fos-focus small{color:#a8e1dd;text-transform:uppercase;font-size:9px;font-weight:900;letter-spacing:.1em}.fos-focus p{margin:6px 0 0;color:var(--soft);font-size:13px;line-height:1.5}
.fos-pillar-grid,.fos-agent-grid,.fos-connector-grid{display:grid;grid-template-columns:1fr;gap:9px}.fos-card,.fos-agent,.fos-connector{padding:15px}.fos-card-top,.fos-row{display:flex;align-items:center;justify-content:space-between;gap:9px;min-width:0}.fos-number{width:34px;height:34px;border-radius:11px;display:grid;place-items:center;background:rgba(213,181,106,.09);border:1px solid rgba(213,181,106,.2);color:var(--gold);font-size:11px;font-weight:950}.fos-card h3,.fos-agent h3,.fos-connector h3{font-size:14px}.fos-card p,.fos-agent p,.fos-connector p{color:var(--muted);font-size:11px;line-height:1.5;margin:8px 0 0}.fos-tags{display:flex;flex-wrap:wrap;gap:6px;margin-top:11px}.fos-tag{display:inline-flex;align-items:center;min-height:27px;padding:0 9px;border:1px solid var(--line);border-radius:999px;background:var(--panel2);color:var(--soft);font-size:9px;font-weight:800}.fos-tag.gold{border-color:rgba(213,181,106,.24);color:var(--gold)}
.fos-list{display:grid;gap:9px}.fos-decision,.fos-alert{padding:16px}.fos-meta{display:flex;flex-wrap:wrap;gap:6px;margin-bottom:8px}.fos-badge{display:inline-flex;align-items:center;min-height:26px;padding:0 9px;border-radius:999px;border:1px solid var(--line);font-size:9px;font-weight:900;text-transform:uppercase;letter-spacing:.05em}.fos-badge.good{color:var(--good);border-color:rgba(118,216,173,.35)}.fos-badge.warn{color:var(--warn);border-color:rgba(239,200,109,.35)}.fos-badge.danger{color:var(--danger);border-color:rgba(255,155,155,.35)}.fos-badge.neutral{color:var(--muted)}.fos-decision h3,.fos-alert h3{font-size:16px;margin:0}.fos-decision p,.fos-alert p{color:var(--muted);font-size:11px;line-height:1.55}.fos-actions{display:flex;flex-wrap:wrap;gap:7px;margin-top:12px}.fos-empty{padding:22px;border:1px dashed var(--line2);border-radius:18px;color:var(--muted);text-align:center;font-size:12px}
.fos-run{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;padding:12px 0;border-bottom:1px solid var(--line)}.fos-run:last-child{border-bottom:0}.fos-run strong{display:block;font-size:12px}.fos-run small{display:block;color:var(--muted);font-size:10px;margin-top:4px;overflow-wrap:anywhere}.fos-time{font-size:9px;color:var(--muted);white-space:nowrap}.fos-probes{display:grid;gap:8px}.fos-probe{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:9px;align-items:center;padding:12px;border:1px solid var(--line);border-radius:14px;background:var(--panel2)}.fos-probe strong{font-size:12px}.fos-probe small{color:var(--muted);font-size:9px}.fos-latency{font-size:10px;color:var(--soft)}
.fos-kflow{display:grid;grid-template-columns:repeat(5,minmax(68px,1fr));gap:6px;overflow-x:auto;padding-bottom:4px}.fos-kstage{min-width:68px;padding:12px 8px;border:1px solid var(--line);border-radius:14px;background:var(--panel2);text-align:center}.fos-kstage b{font-size:18px}.fos-kstage small{display:block;color:var(--muted);font-size:8px;text-transform:uppercase;margin-top:4px}.fos-arrow{color:var(--gold);font-size:10px;line-height:1.4;margin:12px 0}
.fos-bottom{position:fixed;z-index:50;left:8px;right:8px;bottom:calc(8px + env(safe-area-inset-bottom));display:flex;gap:5px;overflow-x:auto;padding:6px;border:1px solid var(--line);border-radius:18px;background:rgba(12,12,16,.96);backdrop-filter:blur(20px);box-shadow:0 20px 60px rgba(0,0,0,.45);scrollbar-width:none}.fos-bottom::-webkit-scrollbar{display:none}.fos-nav-btn{flex:0 0 auto;min-width:64px;min-height:48px;border:0;border-radius:13px;background:transparent;color:var(--muted);font-size:9px;font-weight:850;padding:5px 9px;cursor:pointer}.fos-nav-btn span{display:block;font-size:15px;margin-bottom:3px}.fos-nav-btn.active{background:rgba(213,181,106,.1);color:var(--gold)}
.fos-toast{position:fixed;z-index:80;right:14px;top:74px;max-width:min(360px,calc(100vw - 28px));padding:12px 14px;border-radius:14px;border:1px solid var(--line2);background:#15151b;color:var(--soft);font-size:11px;box-shadow:var(--shadow);opacity:0;transform:translateY(-8px);pointer-events:none;transition:.2s ease}.fos-toast.show{opacity:1;transform:none}.fos-toast[data-tone="error"]{border-color:rgba(255,155,155,.4);color:#ffd0d0}.fos-toast[data-tone="success"]{border-color:rgba(118,216,173,.4);color:#c9f5e2}
@media(min-width:720px){.fos-main{padding:18px 18px 40px}.fos-metrics{grid-template-columns:repeat(4,minmax(0,1fr))}.fos-pillar-grid,.fos-agent-grid,.fos-connector-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.fos-grid.two{grid-template-columns:repeat(2,minmax(0,1fr))}.fos-bottom{left:18px;right:18px}}
@media(min-width:1080px){.fos-shell{grid-template-columns:220px minmax(0,1fr)}.fos-rail{display:flex;position:sticky;top:0;height:100dvh;flex-direction:column;padding:22px 14px;border-right:1px solid var(--line);background:rgba(10,10,13,.74);backdrop-filter:blur(18px)}.fos-rail-logo{padding:4px 8px 20px}.fos-rail-logo b{display:block;font-size:15px}.fos-rail-logo small{color:var(--muted);font-size:9px}.fos-rail-nav{display:grid;gap:5px}.fos-rail .fos-nav-btn{width:100%;display:flex;align-items:center;gap:10px;text-align:left;min-height:45px;padding:0 11px}.fos-rail .fos-nav-btn span{margin:0;font-size:14px}.fos-rail-foot{margin-top:auto;padding:13px;border:1px solid var(--line);border-radius:15px;color:var(--muted);font-size:9px;line-height:1.5}.fos-main{padding:22px 24px 50px}.fos-bottom{display:none}.fos-pillar-grid{grid-template-columns:repeat(3,minmax(0,1fr))}.fos-agent-grid{grid-template-columns:repeat(4,minmax(0,1fr))}.fos-connector-grid{grid-template-columns:repeat(4,minmax(0,1fr))}}
@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}.fos-toast{transition:none}}
`;

const NAV = [
  ['cockpit', '⌂', 'Cockpit'], ['decisions', '✓', 'Decisions'], ['pillars', '◈', 'Piliers'],
  ['agents', 'A', 'Agents'], ['runs', '↻', 'Executions'], ['alerts', '!', 'Alertes'],
  ['connectors', '◎', 'Connecteurs'], ['knowledge', 'K', 'Memoire'], ['automations', '⚙', 'Automations'],
];

function badge(label, tone = 'neutral') {
  return `<span class="fos-badge ${escapeHtml(tone)}">${escapeHtml(label)}</span>`;
}

function statusTone(status) {
  if (['connected', 'online', 'operational', 'succeeded', 'resolved', 'approved'].includes(status)) return 'good';
  if (['critical', 'failed', 'error', 'offline', 'rejected'].includes(status)) return 'danger';
  if (['degraded', 'warning', 'open', 'pending', 'unknown_result', 'acknowledged'].includes(status)) return 'warn';
  return 'neutral';
}

function navButtons() {
  return NAV.map(([id, icon, label], index) => `<button class="fos-nav-btn${index === 0 ? ' active' : ''}" type="button" data-view-target="${id}" aria-label="${escapeHtml(label)}"><span>${escapeHtml(icon)}</span>${escapeHtml(label)}</button>`).join('');
}

function metric(label, value, tone = '') {
  return `<article class="fos-metric"><small>${escapeHtml(label)}</small><b class="${escapeHtml(tone)}">${escapeHtml(value)}</b></article>`;
}

function renderPillars(data) {
  const agents = data.registry?.agents || [];
  return `<div class="fos-pillar-grid">${(data.registry?.pillars || []).map((pillar) => {
    const owned = agents.filter((agent) => agent.pillar === pillar.id);
    return `<article class="fos-card"><div class="fos-card-top"><div><h3>${escapeHtml(pillar.name)}</h3></div><span class="fos-number">0${escapeHtml(pillar.position)}</span></div><p>${escapeHtml(pillar.purpose)}</p><div class="fos-tags">${owned.map((agent) => `<span class="fos-tag gold">${escapeHtml(agent.name)}</span>`).join('') || '<span class="fos-tag">Aucun agent</span>'}</div></article>`;
  }).join('')}</div>`;
}

function renderAgents(data) {
  return `<div class="fos-agent-grid">${(data.registry?.agents || []).map((agent) => `<article class="fos-agent"><div class="fos-row"><h3>${escapeHtml(agent.name)}</h3>${badge(`L${agent.autonomy} · R${agent.maxRisk}`, agent.maxRisk >= 2 ? 'warn' : 'good')}</div><p>${escapeHtml(agent.mission)}</p><div class="fos-tags"><span class="fos-tag gold">${escapeHtml(agent.pillar)}</span><span class="fos-tag">${escapeHtml((data.registry?.sops || []).find((sop) => sop.agent === agent.id)?.label || 'SOP')}</span></div></article>`).join('')}</div>`;
}

function renderDecisions(data) {
  const rows = data.state?.decisions || [];
  if (!rows.length) return '<div class="fos-empty">Aucune validation humaine en attente. Founder OS continue les operations internes autorisees.</div>';
  return `<div class="fos-list">${rows.map((row) => `<article class="fos-decision" data-decision-card="${escapeHtml(row.id)}"><div class="fos-meta">${badge(`R${row.risk ?? '?'}`, Number(row.risk) >= 3 ? 'danger' : Number(row.risk) >= 2 ? 'warn' : 'good')}${badge(row.status || 'pending', statusTone(row.status || 'pending'))}</div><h3>${escapeHtml(row.title || 'Decision')}</h3><p>${escapeHtml(row.why_now || '')}</p>${row.business_impact ? `<p><strong>Impact :</strong> ${escapeHtml(row.business_impact)}</p>` : ''}${row.status === 'pending' ? `<div class="fos-actions"><button class="fos-btn gold" type="button" data-decision-action="approve" data-decision-id="${escapeHtml(row.id)}">Approuver</button><button class="fos-btn danger" type="button" data-decision-action="reject" data-decision-id="${escapeHtml(row.id)}">Refuser</button></div>` : ''}</article>`).join('')}</div>`;
}

function renderRuns(data) {
  const rows = data.state?.runs || [];
  if (!rows.length) return '<div class="fos-empty">Aucune execution Edge enregistree. Lancez « Verifier maintenant » pour executer Studio Monitor.</div>';
  return `<div class="fos-panel">${rows.slice(0, 30).map((row) => `<div class="fos-run"><div><strong>${escapeHtml(row.agent || 'agent')} · ${escapeHtml(row.sop || 'SOP')}</strong><small>${escapeHtml(row.output?.health_status || row.status || 'inconnu')} · L${escapeHtml(row.autonomy ?? '?')} / R${escapeHtml(row.risk ?? '?')}</small></div><span class="fos-time">${escapeHtml(String(row.finished_at || row.started_at || '').slice(0, 16).replace('T', ' '))}</span></div>`).join('')}</div>`;
}

function renderAlerts(data) {
  const rows = data.state?.alerts || [];
  if (!rows.length) return '<div class="fos-empty">Aucune alerte connue.</div>';
  return `<div class="fos-list">${rows.map((row) => `<article class="fos-alert" data-alert-card="${escapeHtml(row.id)}"><div class="fos-meta">${badge(row.severity || 'info', row.severity === 'critical' ? 'danger' : row.severity === 'warning' ? 'warn' : 'neutral')}${badge(row.status || 'open', statusTone(row.status || 'open'))}${row.pillar ? badge(row.pillar, 'neutral') : ''}</div><h3>${escapeHtml(row.summary || 'Alerte')}</h3>${row.details?.safe_next_action ? `<p>${escapeHtml(row.details.safe_next_action)}</p>` : ''}${['open', 'acknowledged'].includes(row.status) ? `<div class="fos-actions">${row.status === 'open' ? `<button class="fos-btn" type="button" data-alert-action="acknowledge" data-alert-id="${escapeHtml(row.id)}">Prendre en compte</button>` : ''}<button class="fos-btn gold" type="button" data-alert-action="resolve" data-alert-id="${escapeHtml(row.id)}">Marquer resolu</button></div>` : ''}</article>`).join('')}</div>`;
}

function renderConnectors(data) {
  return `<div class="fos-connector-grid">${(data.state?.connectors || []).map((row) => `<article class="fos-connector"><div class="fos-row"><h3>${escapeHtml(row.id)}</h3>${badge(row.status || 'unknown', statusTone(row.status))}</div><p>${escapeHtml(row.provider || '')} · ${escapeHtml(row.via || 'adapter')}</p><div class="fos-tags">${(row.capabilities || []).slice(0, 4).map((cap) => `<span class="fos-tag">${escapeHtml(cap)}</span>`).join('')}</div></article>`).join('')}</div>`;
}

function renderKnowledge(data) {
  const stages = data.state?.knowledge?.stages || {};
  return `<div class="fos-grid two"><article class="fos-panel"><h2>Pipeline de connaissance</h2><p class="fos-sub">Source → Signal → Claim → Fact → Memory. Un fait ou une memoire durable doit etre verifie avant promotion.</p><div class="fos-arrow">QUALIFIER → RELIER → VERIFIER → PROMOUVOIR</div><div class="fos-kflow">${['source', 'signal', 'claim', 'fact', 'memory'].map((stage) => `<div class="fos-kstage"><b>${escapeHtml(stages[stage] ?? 0)}</b><small>${escapeHtml(stage)}</small></div>`).join('')}</div></article><article class="fos-panel"><h2>Memoire gouvernee</h2><p class="fos-sub">${escapeHtml(data.state?.knowledge?.note || '')}</p><div class="fos-focus"><small>Principe</small><p>Le cockpit n invente jamais un fait pour remplir une carte. Les donnees inconnues restent inconnues.</p></div></article></div>`;
}

function renderAutomations(data) {
  const health = data.state?.health || {};
  const probes = health.probes || [];
  const outbox = data.state?.outbox || [];
  return `<div class="fos-grid two"><article class="fos-panel"><div class="fos-row"><div><h2>Sante ecosysteme</h2><p class="fos-sub">Dernier controle ${escapeHtml(health.checked_at || 'jamais')}</p></div>${badge(health.status || 'unknown', statusTone(health.status))}</div><div class="fos-probes" style="margin-top:12px">${probes.length ? probes.map((row) => `<div class="fos-probe"><div><strong>${escapeHtml(row.label)}</strong><small>${escapeHtml(row.status)} · HTTP ${escapeHtml(row.http_status)}</small></div><span class="fos-latency">${escapeHtml(row.latency_ms)} ms</span></div>`).join('') : '<div class="fos-empty">Aucun probe execute.</div>'}</div></article><article class="fos-panel"><h2>Outbox verifiee</h2><p class="fos-sub">Une validation place l action dans une file. Aucun engagement externe n est execute directement par un clic navigateur.</p>${outbox.length ? outbox.slice(0, 20).map((row) => `<div class="fos-run"><div><strong>${escapeHtml(row.connector)} · ${escapeHtml(row.action)}</strong><small>${escapeHtml(row.status)} · R${escapeHtml(row.risk)}</small></div><span class="fos-time">${escapeHtml(String(row.created_at || '').slice(0, 16).replace('T', ' '))}</span></div>`).join('') : '<div class="fos-empty">Aucune action externe en attente.</div>'}</article></div>`;
}

function view(id, title, subtitle, body, active = false) {
  return `<section class="fos-view${active ? ' active' : ''}" data-view="${escapeHtml(id)}"><div class="fos-section-head"><div><h1>${escapeHtml(title)}</h1><p>${escapeHtml(subtitle)}</p></div></div>${body}</section>`;
}

export function renderFounderOsDocument(data, csrfToken = '') {
  const metrics = data.metrics || {};
  const brief = data.brief || {};
  const openAlerts = data.state?.alerts?.filter((row) => row.status === 'open') || [];
  const health = data.state?.health || {};
  const connected = metrics.connected_connectors || 0;
  const total = metrics.total_connectors || 0;
  const healthTone = health.status === 'operational' ? '' : health.status === 'critical' ? 'danger' : 'warn';

  const cockpit = `<section class="fos-hero"><div class="fos-eyebrow">Central Supervisor · Autonomous Architecture</div><h1>Founder OS.<br>Une seule tour de controle.</h1><p>6 piliers, 8 agents et 8 SOPs coordonnent l ecosysteme. L analyse, le tri, les controles et les operations internes reversibles s executent sans intervention. Les engagements externes passent par une validation tracee.</p><div class="fos-hero-actions"><button class="fos-btn gold" type="button" data-refresh-founder-os>Verifier maintenant</button><button class="fos-btn" type="button" data-view-jump="decisions">Voir les validations</button><a class="fos-btn" href="/social-intelligence/v5">SOWHAT Control</a></div></section>
  <div class="fos-metrics">${metric('Piliers', String(metrics.pillars ?? 6))}${metric('Agents IA', String(metrics.agents ?? 8))}${metric('Connecteurs actifs', `${connected}/${total}`, connected === total ? 'good' : 'warn')}${metric('Alertes ouvertes', String(metrics.open_alerts ?? openAlerts.length), metrics.critical_alerts ? 'danger' : openAlerts.length ? 'warn' : 'good')}</div>
  <div class="fos-grid two"><article class="fos-panel fos-brief"><div class="fos-row"><div><div class="fos-eyebrow">Command Brief</div><h2 class="fos-brief-headline">${escapeHtml(brief.headline || 'Etat en cours de calcul.')}</h2></div>${badge(health.status || 'unknown', statusTone(health.status))}</div><div class="fos-focus"><small>Priorite maintenant</small><p>${escapeHtml(brief.today_focus || 'Maintenir les flux stables.')}</p></div></article><article class="fos-panel"><h2>Architecture active</h2><p class="fos-sub">SIGNALS → NORMALIZE → CONTEXT → ROUTE → DECIDE → ACT → VERIFY → LOG → LEARN → BRIEF</p><div class="fos-tags"><span class="fos-tag gold">Human gate R2+</span><span class="fos-tag">Idempotence</span><span class="fos-tag">Evidence</span><span class="fos-tag">Fail closed</span></div><div class="fos-focus"><small>Stockage runtime</small><p>${escapeHtml(data.persisted ? 'Miroir prive R2 actif. Supabase reste le control plane durable.' : 'Stockage runtime indisponible : aucune ecriture simulee.')}</p></div></article></div>
  <div class="fos-section-head" style="margin-top:18px"><div><h1>Operating Map</h1><p>Les six responsabilites centrales et leurs agents proprietaires.</p></div></div>${renderPillars(data)}`;

  return `<!doctype html><html lang="fr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="robots" content="noindex,nofollow,noarchive"><meta name="fos-csrf" content="${escapeHtml(csrfToken)}"><title>Founder OS · SOWHAT Ecosystem</title><style>${CSS}</style></head><body><div class="fos-shell"><aside class="fos-rail"><div class="fos-rail-logo"><b>SOWHAT · Founder OS</b><small>Central Supervisor / v${escapeHtml(data.version || '1')}</small></div><nav class="fos-rail-nav" aria-label="Navigation Founder OS">${navButtons()}</nav><div class="fos-rail-foot">Mode prive · preuves avant declaration · aucun secret navigateur · aucune action critique sans gate.</div></aside><main class="fos-main"><header class="fos-top"><div class="fos-brand"><div class="fos-mark">F/OS</div><div><strong>Founder OS</strong><small>SOWHAT Ecosystem · Africa/Casablanca</small></div></div><div class="fos-top-actions"><button class="fos-btn" type="button" data-refresh-founder-os>Refresh</button></div></header><div class="fos-status-strip"><span class="fos-chip"><i class="fos-dot ${healthTone}"></i>${escapeHtml(health.status || 'health unknown')}</span><span class="fos-chip"><i class="fos-dot ${connected ? '' : 'warn'}"></i>${escapeHtml(`${connected}/${total} connecteurs`)}</span><span class="fos-chip"><i class="fos-dot ${metrics.pending_decisions ? 'warn' : ''}"></i>${escapeHtml(`${metrics.pending_decisions || 0} validation(s)`)}</span><span class="fos-chip"><i class="fos-dot ${metrics.critical_alerts ? 'danger' : metrics.open_alerts ? 'warn' : ''}"></i>${escapeHtml(`${metrics.open_alerts || 0} alerte(s)`)}</span></div>${view('cockpit', '', '', cockpit, true)}${view('decisions', 'Decisions', 'Une validation humaine seulement lorsqu un engagement le justifie.', renderDecisions(data))}${view('pillars', '6 Piliers', 'Architecture de responsabilites du Superviseur Central.', renderPillars(data))}${view('agents', '8 Agents IA', 'Autonomie explicite, risque borne, mission observable.', renderAgents(data))}${view('runs', 'Executions', 'Historique des cycles Edge et preuves operationnelles.', renderRuns(data))}${view('alerts', 'Alertes', 'Exceptions uniquement : anomalie, degradation ou controle requis.', renderAlerts(data))}${view('connectors', 'Connecteurs', 'Le statut vert signifie un chemin reellement identifie, jamais une simple intention.', renderConnectors(data))}${view('knowledge', 'Memoire & Knowledge', 'La connaissance progresse par niveaux de preuve.', renderKnowledge(data))}${view('automations', 'Automations', 'Studio Monitor, health probes et outbox des engagements.', renderAutomations(data))}</main></div><nav class="fos-bottom" aria-label="Navigation mobile Founder OS">${navButtons()}</nav><div class="fos-toast" role="status" aria-live="polite" data-fos-toast></div><script src="${FOUNDER_OS_CLIENT_ROUTE}" defer></script></body></html>`;
}
